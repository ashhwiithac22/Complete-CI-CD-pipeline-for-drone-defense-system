apiVersion: v1
kind: Service
metadata:
  name: firewall-app-service
  labels:
    app: firewall-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: firewall-app
